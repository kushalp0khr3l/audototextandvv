*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Whisper model binary (too large for git — download separately)
app/src/main/assets/*.bin

# Temp clone used during setup
/temp_whisper/

# Nested git repo inside whisper.cpp source
app/src/main/cpp/whisper/.git

# ── KittenTTS (TTS backend) ──

# ONNX model + voice files (download from HuggingFace)
app/src/main/assets/model/

# espeak-ng phoneme data (built from source on Linux/WSL)
app/src/main/assets/espeak-ng-data/

# espeak-ng C source (cloned as build dependency)
app/src/main/cpp/espeak-ng/

